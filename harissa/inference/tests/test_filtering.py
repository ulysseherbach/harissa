import sys; sys.path.append('../../../')
from harissa.inference import filtering

# data = np.load('Semrau2017core20.npy')
# d = distance_matrix(data, verb=True)
# f = network_filter(d, verb=True)
# print(f)